<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ASMB.Views.walletlist.export"
            >
    <!--Title="导出"-->
    <VerticalStackLayout Margin="20,0">
        <Label 
            Text="Welcome to ASMB!"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <ImageButton HeightRequest="25" Source="fuzhi.png" Clicked="ImageButton_Clicked" HorizontalOptions="End"></ImageButton>
        <Editor HeightRequest="280"  Margin="0,20,0,0" x:Name="exportedit" AutoSize="TextChanges" ></Editor>

        <Button Margin="0,20,0,0" Text="导出" Clicked="Button_Clicked" ></Button>
        <Label TextColor="Green">1. 导出的数据是加密后的数据，需要密码才能解密</Label>
    </VerticalStackLayout>
</ContentView>